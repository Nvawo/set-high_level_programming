# Python - Inheritance

This project introduces inheritance in Python. It covers creating classes, checking object types, and working with inherited attributes and methods.

## Task 0: Lookup

A function that returns the list of available attributes and methods of an object using the built-in `dir()` function.
